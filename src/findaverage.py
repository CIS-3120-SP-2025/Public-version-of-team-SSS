# find average function
def findaverage(a, b, c):
  # calculate avg of 3 numbers by adding them up and dividing them by 3
  # return float so that decimal places are included
    return float((a + b + c) / 3)
